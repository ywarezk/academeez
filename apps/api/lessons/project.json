{
  "sourceRoot": "apps/api/lessons/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/api/lessons",
        "main": "apps/api/lessons/index.ts",
        "generatePackageJson": true,
        "outputFileName": "index.js",
        "tsConfig": "apps/api/lessons/tsconfig.app.json"
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false
        }
      }
    },
    "serve": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": ["exit 0;"]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/api/lessons/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": ["npm run test:server -- apps/api/lessons"]
      }
    },
    "deploy": {
      "executor": "@nrwl/workspace:run-commands",
      "configurations": {
        "development": {
          "commands": [
            "gcloud beta functions deploy api --set-secrets 'GITHUB_TOKEN=projects/288211122591/secrets/github-token:latest' --set-env-vars BRANCH=dev --runtime nodejs16 --trigger-http --project prj-academeez-dev-bdd2 --entry-point app --source dist/apps/api/lessons --service-account=sa-api-lessons@prj-academeez-dev-bdd2.iam.gserviceaccount.com"
          ]
        }
      }
    }
  },
  "tags": []
}
