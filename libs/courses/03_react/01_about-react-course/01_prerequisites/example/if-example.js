/**
 * if condition and single line if in javascript
 */

// if-else
if (someCondition) {
} else if (anotherCondition) {
} else {
}

// single line if
// this if will return a value
const conditionValue = someCondition ? 'hello' : 42;
