/**
 * You should be familiar with the following logical operators
 */

// if someValue is truthy then hello will get that value, else hello will get the 'hello' string
const hello = someValue || 'hello';

// if anotherValue is truthy the foo will get the 'foo' string, else foo will get anotherValue
const foo = anotherValue && 'foo';
