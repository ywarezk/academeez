/**
 * Spread operator examples
 */

// cloning an object
const myObject = { a: 'hello', b: 'world', c: 'foo' };
// cloned the object and override the c for a new value
const conedObject = { ...myObject, c: 'bar' };
