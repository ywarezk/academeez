import { ZoneFullDirective } from './zone-full.directive';

describe('ZoneFullDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoneFullDirective();
    expect(directive).toBeTruthy();
  });
});
