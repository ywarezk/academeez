/**
 * Constants for the Explore Panel component
 */

/**
 * Delay in milliseconds before closing the explore panel when mouse leaves.
 * This allows users to move their mouse from the button to the panel without
 * the panel closing immediately.
 */
export const EXPLORE_PANEL_CLOSE_DELAY_MS = 100;
