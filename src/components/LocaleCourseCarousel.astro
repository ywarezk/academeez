---

import { CourseCarousel } from './CourseCarousel';

interface Props {
	title: string;
	courseLink: string;
}

let locale = Astro.currentLocale
if (locale === 'en' || !locale) {
	locale = ''
} else {
  locale = `/${locale}`
}
---

<CourseCarousel client:load {...Astro.props} locale={locale} />