{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && corepack prepare pnpm@10.23.0 --activate && node -v && pnpm -v && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build"
}
